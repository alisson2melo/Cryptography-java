Está pasta "example", apresenta dois exemplos simples, de criptografia AES e RSA aplicado com Java.

O arquivo "AES.java", demonstra como criptografar e descriptografar usando o algoritmo AES.
O AES (Advanced Encryption Standard), é um criptosistema em bloco de cifra, de chave simétrica. Ou seja, a mesma chave é usada para criptografia e descriptografia.
Ele funciona como uma cifra de bloco, capaz de lidar com blocos de 128-bits, usando chaves com tamanho de 128, 192 e 256 bits.


O arquivo "RSA.java", demonstra como criptografar e descriptografar usando o algoritmo RSA.
O RSA (Rivest-Shamir-Adleman), é um criptosistema de chave assimétrica, onde uma chave é usada para criptografar (chave pública) e outra para descriptografar (chave privada).
Ele possui chaves em tamanhos: 512-bits, 1024-bits, 2048-bits e 4096-bits.

Por último, temos "Hybrid-Cryptosystem.java". Cujo é um esquema, onde combina os algoritmos AES e RSA.
Você gera um par de chaves aleatórios AES e RSA. Em seguida criptografa a mensagem com a chave simétrica de cifra. Depois criptografa a chave simétrica, com RSA.